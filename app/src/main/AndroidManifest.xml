<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sharinghobby">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.camera2" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OurFriendlyMeeting">
        <activity
            android:name=".MadeGroup3Activity"
            android:label="@string/title_activity_made_group3"
            android:theme="@style/Theme.OurFriendlyMeeting.NoActionBar"></activity>
        <activity android:name=".MadeGroup2Activity" />
        <activity android:name=".MadeGroupActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".SearchAndMake" />
        <activity android:name=".BelongSmallGroup" />
        <activity android:name=".MadeActivity" />
        <activity android:name=".BelongActivity" />
        <activity android:name=".MBselectedActivity" />

        <activity android:name=".CenterActivity" />
        <activity android:name=".FindActivity" />
        <activity android:name=".Join1Activity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>